<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles/main.css">
    <% if (page === 'home') { %>
    <link rel="stylesheet" href="/styles/home.css">
    <% } %>
</head>
<body>
    <header class="main-header" style="background-color: #AE1C33; color: #ECD189;">
        <nav class="nav-container">
            <div class="logo mobile-logo">
                <img src="/images/Quick_logo.png" />
            </div>
            <div class="tickets-btn">
                <a href="/tickets" class="tickets-btn <%= page === 'tickets' ? 'active' : '' %>"><button class="tickets-button">Tickets</button></a>
            </div>
            <button class="hamburger" id="hamburger-menu" aria-label="Open navigation menu">
                <span></span>
                <span></span>
                <span></span>
            </button>
            <button class="close-menu" id="close-menu" aria-label="Close navigation menu">&times;</button>
            <div class="nav-overlay" id="nav-overlay"></div>
            <!-- Desktop nav links -->
            <ul class="nav-links desktop-nav">
                <li><a href="/" class="<%= page === 'home' ? 'active' : '' %>" style="color: #ECD189;">Home</a></li>
                <li><a href="/lineup" class="<%= page === 'lineup' ? 'active' : '' %>" style="color: #ECD189;">Lineup</a></li>
                <li><a href="/involved" class="<%= page === 'involved' ? 'active' : '' %>" style="color: #ECD189;">Get Involved</a></li>
                <li><a href="/about" class="<%= page === 'about' ? 'active' : '' %>" style="color: #ECD189;">About Aubfest</a></li>
            </ul>
            <!-- Mobile slide-in menu -->
            <ul class="slide-menu" id="slide-menu">
                <li><a href="/" class="<%= page === 'home' ? 'active' : '' %>" style="color: #ECD189;">Home</a></li>
                <li><a href="/lineup" class="<%= page === 'lineup' ? 'active' : '' %>" style="color: #ECD189;">Lineup</a></li>
                <li><a href="/involved" class="<%= page === 'involved' ? 'active' : '' %>" style="color: #ECD189;">Get Involved</a></li>
                <li><a href="/about" class="<%= page === 'about' ? 'active' : '' %>" style="color: #ECD189;">About Aubfest</a></li>
            </ul>
        </nav>
    </header>
    <script>
        const hamburgerMenu = document.getElementById('hamburger-menu');
        const closeMenu = document.getElementById('close-menu');
        const navOverlay = document.getElementById('nav-overlay');
        const slideMenu = document.getElementById('slide-menu');

        hamburgerMenu.addEventListener('click', () => {
            navOverlay.classList.toggle('active');
            slideMenu.classList.toggle('active');
        });

        closeMenu.addEventListener('click', () => {
            navOverlay.classList.remove('active');
            slideMenu.classList.remove('active');
        });

        navOverlay.addEventListener('click', () => {
            navOverlay.classList.remove('active');
            slideMenu.classList.remove('active');
        });
    </script>
</body>
</html>