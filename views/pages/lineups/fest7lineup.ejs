<!-- filepath: views/pages/lineups/fest7lineup.ejs -->

<%- include('../partials/header') %>

<!-- Stylesheet-->
<link rel="stylesheet" href="/styles/about.css">
<link rel="stylesheet" href="/styles/lineup.css">

<div class="page-header">
        <h1>AubFest VII Lineup</h1>
</div>

<div class="artist-cards">
  <% artists.forEach(function(artist) { %>
    <div class="artist-card">
      <img src="<%= artist.image %>" alt="<%= artist.name %>">
      <h2><%= artist.name %></h2>
      <p><%= artist.bio %></p>
      <% if (artist.links && artist.links.instagram) { %>
        <a href="<%= artist.links.instagram %>" target="_blank">Instagram</a>
      <% } %>
    </div>
  <% }); %>
</div>

<%- include('../partials/footer') %>